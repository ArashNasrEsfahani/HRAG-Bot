## Taxonomy Relabel

You are proposing a better, more descriptive label for a category node in a
document taxonomy. The new label should accurately capture the shared topic of
the documents inside it.

### Rules
- Output ONE LINE of plain text — the new label only.
- No quotes, no preamble, no "Label:" prefix, no markdown, no trailing
  punctuation.
- Maximum 60 characters.
- Use Title Case.
- Prefer concrete topical phrasing (e.g. "Graph Neural Networks") over vague
  generalities (e.g. "AI Stuff").

### Context

**Current label:** {current_label}

**Current description:** {description}

**Member document summaries:**
{member_summaries}

### Your output
